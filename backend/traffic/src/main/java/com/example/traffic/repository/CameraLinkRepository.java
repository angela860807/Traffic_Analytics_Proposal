package com.example.traffic.repository;

import com.example.traffic.domain.CameraLink;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CameraLinkRepository extends JpaRepository<CameraLink, Long> {
}
