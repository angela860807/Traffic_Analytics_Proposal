# 예지보전 병합 검증 작업 정리 - 2026-06-21

## 1. 작업 배경

2026-06-19 기준 `main`에 병합된 예지보전 운영 API, FastAPI Rule 평가, `/admin/ops` 프론트 연동 내용을 실제 Docker 환경에서 확인하고, 시연 화면의 운영자 가독성을 개선했다.

브랜치는 변경하지 않았고, 사용자가 빌드를 담당하기로 했으므로 Codex는 빌드를 실행하지 않았다.

## 2. 확인한 실행 환경

- Docker compose 서비스:
  - `postgres-db`
  - `spring-backend`
  - `fastapi-server`
  - `frontend`
- 주요 접속:
  - Spring Boot: `http://localhost:8080`
  - Frontend: `http://localhost:5174`
- 관리자 로그인:
  - `admin@email.com / 1234`
- 예지보전 내부 API key:
  - `traffic-ai-internal-key-2026`

## 3. E2E 검증 결과

### 3-1. 상태 샘플 주입

사용한 스크립트:

```powershell
.\tools\predictive_demo\import_health_samples.ps1 `
  -CsvPath "tools\predictive_demo\camera-health-rule-trigger-samples.csv" `
  -BaseUrl "http://localhost:8080" `
  -InternalApiKey "traffic-ai-internal-key-2026"
```

확인 내용:

- `bad_fps_1` ~ `bad_fps_4` 상태 샘플 import 성공
- 샘플 import 후 Rule detector가 anomaly/ticket을 생성하는 흐름 확인
- `anomaly_events` 6건 생성 확인
- `maintenance_tickets` 3건 생성 확인

### 3-2. API 검증

관리자 토큰 발급:

```powershell
$login = Invoke-RestMethod `
  -Method Post `
  -Uri "http://localhost:8080/api/auth/login" `
  -ContentType "application/json" `
  -Body (@{
    email = "admin@email.com"
    password = "1234"
  } | ConvertTo-Json)

$headers = @{ Authorization = "Bearer $($login.data.accessToken)" }
```

확인한 API:

```powershell
Invoke-RestMethod `
  -Uri "http://localhost:8080/api/v1/predictive/summary?dataSource=REAL" `
  -Headers $headers

Invoke-RestMethod `
  -Uri "http://localhost:8080/api/v1/predictive/anomaly-events?dataSource=REAL&page=0&size=20&sort=firstDetectedAt,desc" `
  -Headers $headers

Invoke-RestMethod `
  -Uri "http://localhost:8080/api/v1/predictive/maintenance-tickets?page=0&size=20&sort=createdAt,desc" `
  -Headers $headers
```

확인 결과:

- summary 응답 정상
- anomaly-events totalElements 6 확인
- maintenance-tickets totalElements 3 확인
- 정비 상태 변경 API 동작 확인
- 이상 이벤트 resolve API 동작 확인

### 3-3. 화면 검증

`/admin/ops`에서 확인한 내용:

- 장비 상태 카드가 실제 summary/cameras API 결과를 사용
- 이상 이벤트 및 정비 건 현황이 실제 anomaly/ticket API 결과를 사용
- 상세 버튼 클릭 시 선택한 이상 이벤트가 상단 상세 카드에 반영
- 배정/작업 시작/해결/종결 흐름은 하단 현황표에서 처리하도록 정리

## 4. 로컬 DB 보정 이슈

`008_predictive_seed_policies.sql`을 기존 Docker DB에 직접 적용할 때 다음 문제가 있었다.

- `detector_versions.metrics_json` not-null 제약 위반
- `detector_versions.created_at` default 미비
- `anomaly_policies.config_json`, `threshold_direction`, `created_at`, `updated_at` default 미비
- `maintenance_ticket_number_seq` 존재 보장 필요

검증 중 임시로 확인한 보정 방향:

```sql
ALTER TABLE detector_versions
  ALTER COLUMN metrics_json SET DEFAULT '{}'::jsonb,
  ALTER COLUMN created_at SET DEFAULT CURRENT_TIMESTAMP;

ALTER TABLE anomaly_policies
  ALTER COLUMN config_json SET DEFAULT '{}'::jsonb,
  ALTER COLUMN created_at SET DEFAULT CURRENT_TIMESTAMP,
  ALTER COLUMN updated_at SET DEFAULT CURRENT_TIMESTAMP,
  ALTER COLUMN threshold_direction SET DEFAULT 'HIGHER_IS_WORSE';

CREATE SEQUENCE IF NOT EXISTS maintenance_ticket_number_seq;
```

주의:

- 위 SQL은 작업 중 원인 확인용으로 사용한 보정 방향이다.
- 최종적으로는 migration/seed 파일에 재현 가능하게 반영해야 한다.

## 5. 코드 변경 요약

### 5-1. Frontend

주요 파일:

- `trafficAS-b/src/views/admin/OpsView.vue`
- `trafficAS-b/tests/usePredictivePerm.test.js`

요약:

- `/admin/ops` 예지보전 운영 화면 가독성 개선
- 이상 이벤트 상세 카드 한글화
- 지표 단위 정규화
- 정비 처리 액션을 하단 현황표로 일원화
- `티켓` 화면 용어를 `정비 건`으로 변경
- `SLA 잔여`를 `대응 제한시간`으로 변경
- local fallback 토큰으로 `/admin/ops`에 접근하지 못하도록 테스트 보강

프론트 상세 변경은 별도 문서:

- `docs/phase2-predictive-maintenance/Frontend_admin_ops_UI_변경사항_2026-06-21.md`

### 5-2. Spring Boot 테스트 보정

주요 파일:

- `backend/traffic/src/test/java/com/example/traffic/controller/DetectionLogControllerIntegrationTest.java`

변경:

- OCR_FAILED 통합 테스트에서 최근 100건 검색 대신 응답의 `$.data.logId`를 읽어 해당 row를 직접 조회하도록 수정
- 테스트가 같은 이미지 URL 또는 가까운 시간대 row에 흔들리지 않게 보정

### 5-3. Demo profile 추가

주요 파일:

- `backend/traffic/src/main/resources/application-demo.yml`

내용:

```yaml
spring:
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
```

목적:

- demo 환경에서 Hibernate 자동 DDL보다 migration 기반 schema 검증에 가깝게 운영하기 위한 profile 초안

## 6. 현재 검증 상태

Codex가 실행한 검증:

```powershell
git diff --check
```

결과:

- 공백 오류 없음
- Windows CRLF 경고만 확인

실행하지 않은 검증:

- frontend build
- backend build
- 전체 test

사용자가 빌드 담당이므로 빌드는 실행하지 않았다.

## 7. 다음 TODO

### 우선순위 높음

1. 프론트 재빌드 후 `/admin/ops` 화면 재확인

```powershell
docker compose up -d --build frontend
```

확인 항목:

- 상세 클릭 시 선택 행 표시가 셀마다 끊기지 않는지
- 상단 상세 카드가 선택한 이상과 연관 조치 지표를 명확히 보여주는지
- `정비 건`, `정비 상태`, `대응 제한시간` 용어가 일관되게 보이는지
- ratio/rate/score 단위가 각각 `%`로 보이는지

2. 새 DB에서 migration/seed 재현성 정리

확인 항목:

- `008_predictive_seed_policies.sql` 단독 재실행 가능 여부
- `detector_versions` default 누락 보정
- `anomaly_policies` default 누락 보정
- `maintenance_ticket_number_seq` 생성 위치 확정

3. 정비 담당자 후보 API 또는 seed 정리

현재 화면 후보는 로컬 DB에 존재하는 `member_id=2 관리자`, `member_id=1 이용자` 기준으로 맞췄다.

다음 중 하나로 정리해야 한다.

- 정비 담당자/운영자 목록 API 추가
- demo seed에 MAINTAINER 계정 추가
- assign API가 role 제한을 검증한다면 프론트 후보도 role 기준으로 필터링

### 우선순위 중간

4. 정비 변경 이력 실제 API 연동

현재 모달 timeline은 현재 상태 기반 프론트 추정이다.

추가 권장 API:

```text
GET /api/v1/predictive/maintenance-tickets/{ticketId}/histories
```

5. Event lifecycle 고도화

- 정상 3회 연속 감지 시 `RECOVERED` 자동 전환
- 재발 시 recurrence 증가
- WARNING 지속 시 P2 정비 건 생성 정책 확정

6. Summary KPI 정교화

- MTTA/MTTR 실제 이력 기반 계산
- 대응 지연 기준을 `dueAckAt`과 `dueStartAt` 중 어떤 단계로 표시할지 명확화

### 우선순위 낮음

7. SHADOW 비교 모델 표시 고도화

- topFeatures 실제 응답 연동
- 비교 모델이 운영 이벤트를 생성하지 않는다는 설명 유지

8. 문서 최신화

- `04_Frontend_작업_TODO.md`의 완료 항목 체크 반영
- API 계약서에 화면 표시 용어와 API enum 용어 차이 명시

## 8. 재빌드 명령

프론트만:

```powershell
docker compose up -d --build frontend
```

전체:

```powershell
docker compose up -d --build spring-backend fastapi-server frontend
```

## 9. 커밋 전 확인 명령

```powershell
git status --short
git diff --stat
git diff --check
```
